name: POJA Import CI

on:
  push:
    branches:
      - import-5adced34-5909-4926-bb96-24c572a24c68-pre-transformation
env:
  ORG_ID: ad22f22c-bcbe-4872-91a5-aae017615d4b
  IMPORT_ID: 5adced34-5909-4926-bb96-24c572a24c68

jobs:
  pre-transformation-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    

    steps:
      - uses: actions/checkout@v4.1.6

      - uses: actions/setup-java@v4.2.1
        with:
          java-version: '21'
          distribution: 'corretto'

      - name: Run tests and capture outcome
        id: test
        continue-on-error: true
        run: |
          set -o pipefail
          set +e

          if [ -f "build.gradle" ]; then
            chmod +x gradlew
            ./gradlew test
          elif [ -f "pom.xml" ]; then
            chmod +x mvnw
            ./mvnw test
          else
            echo "No build tool detected!" >&2
            echo "outcome=FAILURE" >> $GITHUB_OUTPUT
            exit 1
          fi

          EXIT_CODE=$?
          set -e

          if [ $EXIT_CODE -eq 0 ]; then
            echo "outcome=SUCCESS" >> $GITHUB_OUTPUT
          else
            echo "outcome=FAILURE" >> $GITHUB_OUTPUT
          fi

      - name: Print test failures
        if: always()
        run: |
          find build/test-results -name "*.xml" | xargs grep -l "<failure\|<error" 2>/dev/null | \
          while read f; do
            echo "=== $f ==="
            grep -A 20 "<failure\|<error" "$f"
          done

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pre-transformation-test-report
          path: build/reports/tests/test/    

      - name: Update import state
        run: |
          REPO_NAME=$(basename "${GITHUB_REPOSITORY}")

          curl --fail -X POST "https://ado72jc2pqlookhy5ohhsnpwvq0uidrm.lambda-url.eu-west-3.on.aws/orgs/${{ env.ORG_ID }}/imports/${{ env.IMPORT_ID }}/states" \
            -H "Authorization: AppImportBearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "$(jq -n \
                  --arg job_name "PRE_TRANSFORMATION_TEST" \
                  --arg job_status "${{ steps.test.outputs.outcome }}" \
                  --arg run_id "${{ github.run_id }}" \
                  --arg attempt_nb "${{ github.run_attempt }}" \
                  --arg repo_owner "${{ github.repository_owner }}" \
                  --arg repo_name "$REPO_NAME" \
                  '{ job_name: $job_name, job_status: $job_status, run_id: $run_id, attempt_nb: $attempt_nb, repo_owner: $repo_owner, repo_name: $repo_name }')"
